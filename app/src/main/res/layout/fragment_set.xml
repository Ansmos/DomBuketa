<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settings_fragment_root"
    tools:context=".views.fragments.SetFragment">

    <include
        android:id="@+id/switch_dark_mode"
        layout="@layout/switch_settings"
        />

    <Button
        android:id="@+id/button_clear_visited"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/switch_dark_mode"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="@dimen/fragment_set.layout_margin"
        android:layout_marginLeft="@dimen/fragment_set.layout_margin"
        android:text="@string/fragment_set.clear_visited">
    </Button>

</androidx.constraintlayout.widget.ConstraintLayout>